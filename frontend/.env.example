# Copy to frontend/.env.local and fill in. Never commit real values here.

# Public URL of the deployed BACKEND API (no trailing slash, no path).
# This is the backend Vercel project / Node app — NOT the frontend's own URL.
# Locally this can be omitted — next.config.js defaults to localhost:4000.
BACKEND_URL="https://your-backend.vercel.app"
