# Public URL of the deployed backend Node.js Web App (no trailing slash).
# Locally this can be omitted — next.config.js defaults to localhost:4000.
BACKEND_URL="https://seo-ledger-frontend.vercel.app/dashboard"
